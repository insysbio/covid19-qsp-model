/*
  Entry point for using this module as part of integrated model
*/

// @Const
include ./mph_life_cycle.xlsx type xlsx with { sheet: 1, omitRows: 3 }
// @Record
include ./mph_life_cycle.xlsx type xlsx with { sheet: 2, omitRows: 3 }
// @Compartment
include ./mph_life_cycle.xlsx type xlsx with { sheet: 3, omitRows: 3 }
// @Species
include ./mph_life_cycle.xlsx type xlsx with { sheet: 4, omitRows: 3 }
// @Reaction
include ./mph_life_cycle.xlsx type xlsx with { sheet: 5, omitRows: 3 }
